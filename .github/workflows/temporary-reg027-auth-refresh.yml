name: Temporary REG-027 auth migration refresh

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Retired temporary transformer; delete this file next."
