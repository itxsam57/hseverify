name: Temporary EMA-43 Worker-link locator autopatch

on:
  push:
    branches:
      - feat/m2-06-assessment-catalogue-eligibility
    paths:
      - .github/workflows/zz-temp-autopatch-ema-43.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact feature branch
        uses: actions/checkout@v6
        with:
          ref: feat/m2-06-assessment-catalogue-eligibility
          fetch-depth: 0

      - name: Apply exact Worker-link locator repair
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path("scripts/hard-browser-retrospective-company-registration.mjs")
          text = path.read_text(encoding="utf-8")
          old = '    const existingLinkCard = page.locator("article").filter({ hasText: EXISTING_WORKER_EMAIL }).filter({ hasText: "email invitation" }).first();'
          new = '    const existingLinkCard = page.locator("article").filter({ hasText: EXISTING_WORKER_EMAIL }).filter({ hasText: "Source: invitation" }).first();'

          count = text.count(old)
          if count != 1:
              raise SystemExit(f"Expected exactly one obsolete Worker-link locator, found {count}")

          path.write_text(text.replace(old, new), encoding="utf-8")
          PY

      - name: Remove temporary autopatch workflow
        run: git rm .github/workflows/zz-temp-autopatch-ema-43.yml

      - name: Commit exact harness repair
        shell: bash
        run: |
          git config user.name "HSE Verify Engineering Factory"
          git config user.email "engineering-factory@users.noreply.github.com"
          git add scripts/hard-browser-retrospective-company-registration.mjs
          git commit -m "test: match current Worker-link source label [autopatch-ema-43]"
          git push origin HEAD:feat/m2-06-assessment-catalogue-eligibility
