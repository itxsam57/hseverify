name: M1.12 concern green v2 one-shot

on:
  push:
    branches:
      - build/m1-12-public-verification-foundation

permissions:
  contents: write

concurrency:
  group: m1-12-concern-green-v2-once
  cancel-in-progress: false

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Stage concern implementation and concurrency correction
        run: |
          python scripts/patch-m1-12-concern-green.py
          python scripts/patch-m1-12-concern-concurrency.py
      - name: Verify M1.12 concern implementation
        run: |
          git diff --check
          timeout 180s node scripts/run-public-verification-tests.mjs
          npm run typecheck
      - name: Commit verified product change and self-clean
        shell: bash
        run: |
          git rm \
            .github/workflows/m1-12-concern-green-once.yml \
            .github/workflows/m1-12-concern-green-v2-once.yml \
            scripts/patch-m1-12-concern-green.py \
            scripts/patch-m1-12-concern-concurrency.py
          git add \
            src/lib/audit/audit-domain.ts \
            database/migrations/0031_public_verification_foundation.up.sql \
            src/lib/public-verification/public-verification-repository.ts \
            src/lib/public-verification/public-verification-service.ts
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'feat: add idempotent public concern intake'
          git push origin HEAD:build/m1-12-public-verification-foundation
