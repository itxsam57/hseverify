-- M1.07 Subunit 6 correction history is immutable durable assurance evidence.
-- Rollback is deliberately logical/monotonic: correction versions, requests,
-- decisions and evidence lineage remain so an older local/test release cannot
-- erase accepted history or make a rejected/accepted correction ambiguous.
SELECT 1;
