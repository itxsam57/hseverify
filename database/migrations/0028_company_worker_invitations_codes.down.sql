-- M1.10 workforce secrets, consent links, audit vocabulary and history are
-- monotonic security guarantees. Rolling the migration ledger back must not
-- weaken accepted constraints or remove historical rows.
SELECT 1;
