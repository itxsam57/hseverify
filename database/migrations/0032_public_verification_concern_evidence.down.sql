-- M1.12 public concern evidence is compliance/security history and remains monotonic.
-- Logical rollback removes only the migration ledger entry. The disabled non-login
-- storage principal, secure-file authority guard, candidate history and terminal
-- scan bindings remain so immutable M1.06 files never lose provenance and a
-- deterministic reapply can verify the same schema without deleting evidence.
SELECT 1;
