# HSE Verify — Regression Register

Stable regression IDs must remain even when a feature is rebuilt. `PROTECTED` means an automated test or permanent source contract currently guards the defect. `FUTURE GUARD REQUIRED` means the serious defect occurred in the discarded prototype or an unbuilt workflow and must receive an automated test when that feature is implemented.

| ID | Defect prevented | Role/feature | Root cause | Expected behaviour | Automated test | Manual spot-check | Status |
|---|---|---|---|---|---|---|---|
| REG-001 | A signed-in role opens another role's dashboard | All portals | Route/UI isolation without authoritative server role checks | Direct copied URLs never display the other portal and do not switch the session role | `authentication-portal-isolation.test.mjs`; central authorization tests | Copy one affected cross-role URL when auth/authorization changes | PROTECTED |
| REG-002 | Worker registration OTP transition goes blank, stalls, or bypasses the intended POST state transition | Worker registration | Fragile transition/render coupling | Each OTP step uses a challenge-bound normal POST and preserves the continuation state | `worker-registration-owner-repair.test.mjs`; registration route/SQL tests | Register a synthetic Worker when registration UI changes | PROTECTED |
| REG-003 | Opening `/worker/dashboard` after logout shows global “Not available” instead of Worker login | Worker logout/routing | Redirect occurred during protected App Router rendering and exposed the wrong boundary | Missing-cookie request receives pre-render `307` to fixed Worker login; no protected/not-found HTML | `authentication-signed-out-proxy.test.mjs`; `smoke-signed-out-portal-redirects.mjs` | Logout and paste one changed protected route | PROTECTED |
| REG-004 | Route URL changes but page content remains stale until manual refresh | Shared navigation | Prototype client routing/state was not connected to the rendered route | Navigation renders the intended view without accidental refresh | Current Next route/runtime checks; add feature-specific UI test for new navigation | Navigate normally and confirm no refresh is needed | PROTECTED BASE / EXPAND PER FEATURE |
| REG-005 | Worker Profile or shared shell causes page-wide horizontal overflow or clipped action controls | Worker/shared UI | Missing shrink containment and table-only overflow boundary | Page remains contained at accepted desktop, tablet, mobile, and zoom widths | `profile-overflow-contract.test.mjs`; design/UX checks | Responsive visual check after shared CSS/shell changes | PROTECTED |
| REG-006 | Revoked, expired, stale, inactive-account, inactive-tenant, or inactive-membership sessions continue | All protected access | Lifecycle state not resolved centrally or fail-open ordering | Every invalid lifecycle state is denied deterministically and non-enumeratingly | Authorization context domain and exact SQL tests; auth completion tests | Logout/reset visible flow when auth changes | PROTECTED |
| REG-007 | Root invitation tests fail or behaviour changes merely because a hard-coded calendar date passes | Root bootstrap/invitations | Clock-dependent fixed test expiry | Tests derive coherent future/past timestamps and product enforces real expiry | `authentication-invitation-expiry.test.mjs` | None | PROTECTED |
| REG-008 | Authorization code passes isolated tests but fails Next runtime because emitted `.js` imports or obsolete TypeScript resolution are used | Authorization/build | Test compiler semantics diverged from live Next source | Live imports are runtime-compatible and isolated compiler uses supported `Node16` semantics | Authorization source-contract checks; strict typecheck/build | None | PROTECTED |
| REG-009 | Build/typecheck rewrites tracked Next/TypeScript configuration or leaves generated workspaces | Engineering/build | Shared generated configuration/output paths | Every Next mode uses isolated generated config/output and source config remains unchanged | `next-build-system.test.mjs`; development/build scripts | Final Git clean-state check only when owner acceptance requires it | PROTECTED |
| REG-010 | A selected PDF or image leaks into another evidence form or wrong record | Future upload | Shared stale client file state | Every form has independent upload state and stored object remains bound to the intended record | Required future M1.06 uploader state and repository tests | Upload different PDF/PNG files to separate forms | FUTURE GUARD REQUIRED |
| REG-011 | PDF upload fails while PNG works, or extension/MIME/signature disagree | Future upload | Extension-only or browser-only validation | PDF, PNG, and JPEG are validated by size, MIME and signature at trusted boundaries | Required future M1.06 validation tests | Upload accepted and mismatched sample files | FUTURE GUARD REQUIRED |
| REG-012 | Reviewer sees a submission without Worker name/file preview, or review opens a white screen | Future verification | Incomplete record projection, unsafe preview route, or client render failure | Correct Worker, type, version and authorized file preview display reliably | Required future M1.06/M2.02 repository/API/preview tests | Worker submit then Verifier open/preview | FUTURE GUARD REQUIRED |
| REG-013 | Worker submission stays “Reviewing” after reviewer action, or disappears from reviewer without updating Worker | Future verification status | Non-transactional or disconnected status transitions | Reviewer decision and Worker-visible status/history commit atomically | Required future verification lifecycle tests | Cross-role submit/review/refresh | FUTURE GUARD REQUIRED |
| REG-014 | Written assessment answers disappear, assessment becomes stuck, or safe exit/recovery fails | Future assessments | Answers were client-only and workflow state lacked durable recovery | Every answer persists before next question; resume and safe interruption follow the state machine | Required future M2 assessment persistence/interruption tests | Enter text, refresh/reopen, resume and complete | FUTURE GUARD REQUIRED |
| REG-015 | A Worker receives repeated questions despite non-repetition rules | Future assessments | Random selection without durable exposure history | Question selection is randomized within policy and excludes prior Worker exposure when required | Required future question-bank/form-generation database tests | Run multiple synthetic attempts per policy | FUTURE GUARD REQUIRED |
| REG-016 | Notifications are non-clickable or open the wrong role/page | Future notifications | Demonstration-only notification data or missing deep-link contract | Persisted notification opens the exact authorized destination without role crossing | Required future M1.05 notification/deep-link tests | Click each changed notification type | FUTURE GUARD REQUIRED |
| REG-017 | Visible buttons/options are decorative, duplicate, or mutate labels without completing work | Shared/future workflows | UI created before server command/state | Every visible control has a defined command, failure state and persisted result, or is absent/disabled honestly | `NO_DECORATIVE_CONTROLS` source contracts plus feature tests | Use every changed visible control | PROTECTED POLICY / EXPAND PER FEATURE |
| REG-018 | Company tenant A reads, writes, checks existence, or locks a record belonging to tenant B | Company tenant data | Record-ID-only query or fetch-global-then-filter | Every tenant-owned SQL operation includes principal-derived tenant scope; cross-tenant, missing and malformed identifiers are indistinguishable | `authorization-tenant-scope-repository.test.mjs`; `authorization-final-isolation-suite.test.mjs` | Future real tenant-owned surfaces add feature-specific spot-checks | PROTECTED BASE / EXPAND PER REAL REPOSITORY |
| REG-019 | Concurrent Company commands pass authorization then mutate or read after session, account, tenant, membership, role or permission state changes | Company tenant operations | Authorization and data operation occur in separate uncontrolled transactions | Reads and writes revalidate and lock trusted session/account/tenant/membership/permission context inside the same transaction | `authorization-tenant-scope-concurrency.test.mjs`; `authorization-final-isolation-suite.test.mjs` | Usually automated; future visible command flow when implemented | PROTECTED BASE / EXPAND PER REAL COMMAND |
| REG-020 | Engineering handoff declares readiness when the full gate failed or lists only filenames instead of visible impact | Owner/engineering | No durable result/status decision or project mapping | Required failure yields `NOT READY`; passed engineering-only changes yield `NO MANUAL FEATURE TEST REQUIRED`; visible changes produce exact role-based steps | `tests/engineering/handoff-domain.test.mjs`; engineering contract check | Read the generated CI summary | PROTECTED |
| REG-021 | Company tenant demonstration accepts a tenant/scope selector from the browser or shows another tenant's record | Company tenant-scope demonstration | Page/action code bypasses the trusted current-membership principal or exposes unscoped identifiers | Browser sends only neutral record identity/content; page/actions derive tenant and permissions server-side; cross-tenant and missing results are identical | `check-company-scope-demonstration.mjs`; `authorization-company-scope-demonstration.test.mjs` | Accepted Company CRUD/Worker copied-route workflow | PROTECTED — OWNER PASS |
| REG-022 | Company demonstration create/update/delete appears only after a manual refresh, double-submits, or exposes a destructive action without confirmation | Company tenant-scope demonstration | Server action state is not connected to route revalidation/pending state or destructive confirmation | Pending controls disable duplicate submission; successful commands refresh server data automatically; delete requires confirmation and returns an explicit result | Company-scope source contract; production build; generated exact handoff | Accepted create/update/delete workflow | PROTECTED — OWNER PASS |
| REG-023 | Delete confirmation opens but the destructive command does nothing or submits the surrounding edit form | Shared destructive confirmation / Company tenant-scope demonstration | A confirmation action form was rendered inside an existing edit form, creating invalid nested-form browser behaviour | Dialog trigger never submits the caller form; cancel is non-destructive; confirmed action runs from a body-portal form, shows a pending state, and cannot double-submit | `check-company-scope-demonstration.mjs`; strict TypeScript/build gate | Accepted cancel/confirm/delete workflow | PROTECTED — OWNER PASS |
| REG-024 | A newly protected route bypasses the pre-render signed-out redirect and exposes a not-found/protected-render boundary | All accepted protected routes | Proxy matcher covered only a subset of routes | All 11 accepted protected routes return minimal fixed-role `307` redirects before rendering; database authorization remains authoritative for cookie-present requests | `smoke-signed-out-portal-redirects.mjs`; protected endpoint inventory test | One representative changed route | PROTECTED |
| REG-025 | A fixed role is accidentally allowed into another fixed portal | All six portals | Partial pair testing misses a role combination | Six own-role entries pass and all 30 cross-role combinations return central `role_mismatch` without changing the session | `authorization-cross-role-matrix.test.mjs` | One representative copied URL at final closure | PROTECTED |
| REG-026 | M1.04 rollback removes or corrupts M1.01–M1.03 data, or cannot reapply after restart | Platform migrations | Only latest-migration rollback was tested in isolation | `0006` then `0005` roll back in order; base Worker Profile/auth data remains; both reapply idempotently with matching checksums in disposable and reopened persistent PGlite | `authorization-m1-04-migration-stack.test.mjs` | No destructive manual database test | PROTECTED |
| REG-027 | A previously accepted migration test fails whenever a newer migration becomes globally latest | Platform migration regression suites | Tests hard-coded a specific migration as the repository-wide latest instead of proving the migration they own beneath newer layers | Generic migration tests derive the manifest dynamically; owned authentication/authorization/M1.04/audit/outbox/notification tests roll newer layers back first, verify their own boundary, then reapply the exact owned stack | `platform-foundation.test.mjs`; `authentication-foundation.test.mjs`; `authorization-tenant-foundation.test.mjs`; `authorization-m1-04-migration-stack.test.mjs`; `audit-migration-stack.test.mjs`; `outbox-migration-stack.test.mjs`; `notification-migration-stack.test.mjs` | None | PROTECTED |
| REG-028 | An expired or reclaimed email worker starts or finalizes a delivery attempt, or a replacement attempt jumps delivery state incorrectly | M1.05 email delivery | Attempt tests treated stale SQL filtering as an exception and did not exercise a real started-attempt reclaim path | Expired leases produce no start row; a started attempt becomes `lease_expired`, delivery reconciles to retry state, the new lease advances the attempt number, and the stale worker cannot finalize | `email-delivery-foundation.test.mjs`; `email-delivery-runtime.test.mjs`; email-delivery repository exact lease predicates and migration triggers | None | PROTECTED |
| REG-029 | Re-entering an already-created live email attempt duplicates its immutable `attempt.started` audit fact | M1.05 email delivery | Handler treated every running attempt projection as newly created | Repository exposes whether the exact attempt row was newly created; start audit is appended only for a newly-created attempt | `check-email-delivery-foundation.mjs`; `email-delivery-foundation.test.mjs`; `email-delivery-runtime.test.mjs`; unique source outbox attempt | None | PROTECTED |
| REG-030 | A reclaimed outbox job dispatches an email again after the email was already durably delivered or terminally failed before central outbox completion | M1.05 email delivery | Handler did not short-circuit persisted terminal delivery state on a later reclaimed lease | Persisted delivered state returns outbox success without another email attempt/provider call; persisted terminal failure returns the stored terminal result; no second attempt row is created | `check-email-delivery-foundation.mjs`; `email-delivery-foundation.test.mjs`; `email-delivery-runtime.test.mjs`; repository terminal-state preparation contract | None | PROTECTED |
| REG-031 | A real server-module runtime regression test fails before execution because its isolated compiler invents module/declaration semantics that differ from the application | Engineering / M1.05 email runtime proof | The first runtime harness used a standalone Node16 tsconfig, requiring undeclared `server-only`, forcing CommonJS resolution across an application `.mjs` import, and relying on deprecated `baseUrl` | Runtime proof executes temporary CommonJS copies produced with TypeScript `transpileModule` only after normal project typechecking remains authoritative; the temporary copy strips only the exact `import "server-only"` marker and injects only explicit database/environment factories; no standalone runtime tsconfig/baseUrl/compiler-resolution layer exists | `check-email-delivery-foundation.mjs`; `run-email-delivery-runtime-tests.mjs`; full project `typecheck` + production `build` + `test:email-delivery-runtime` in `npm run check` | None | PROTECTED |
