# Merge Policy

Merge this branch only after the Worker foundation checks workflow succeeds or after an explicitly documented human review accepts the remaining validation risk. Prefer a squash merge so the clean rebuild lands on `main` as one auditable Worker Dashboard foundation change.
