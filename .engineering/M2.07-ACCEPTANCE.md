# Engineering Factory Acceptance Certificate — M2.07

**Task:** M2.07 — Candidate Assessment Window  
**Operating depth:** CRITICAL  
**Work Contract:** `.engineering/M2.07-WORK-CONTRACT.md`  
**Evidence head:** `9727d56261d51f7a2c6f61053ccb221d20e529d5`  
**Base tested against:** `a3ee0381bc482e5ba49c728f80b9cdc0eb01b6cb`  
**Gatekeeper verdict:** `ACCEPT`  
**Factory state:** `ACCEPTED`

> This certificate accepts M2.07 only. It does not mean the PR is already merged, does not implement M2.08 interruption recovery, M2.09 integrity/proctoring, or M2.10 scoring/review, and does not claim full-product production readiness.

## Scope accepted

- authenticated Worker-owned assessment begin over backend-authoritative M2.06 eligibility;
- one persistent attempt per immutable M2.05 generated form with exact case/Worker/catalogue/blueprint/form lineage;
- one-question-only pinned delivery with future-question, answer-key, rubric and scoring secrecy;
- all six canonical answer types with server-authoritative normalization and validation;
- atomic answer commit before progression, duplicate-idempotent replay and conflict-safe stale submissions;
- database append-only protection for committed answer rows;
- final `SUBMITTED` attempt receipt while the Assurance Case deliberately remains `Assessment in progress`;
- native start/submit audit and timeline evidence with no answer-content metadata;
- real Worker Available Assessments → Start assessment → MCQ → written answer → reload → final submit workflow;
- strict Server→Client allowlisted attempt projection with no internal Worker/form/lifecycle metadata leakage;
- cross-Worker direct attempt access denial;
- permanent `check:m2-07` and `test:m2-07` commands plus Full Engineering integration;
- rollback/reapply behavior that removes only M2.07 schema, restores the pre-M2.07 audit action vocabulary, preserves historical attempt audit evidence and rejects new M2.07 actions after rollback.

Explicitly outside M2.07: uncommitted autosave/interruption/emergency recovery, webcam/microphone/screen integrity, secure-window enforcement, timers, scoring/correctness/pass-fail, written rubric evaluation, reviewer allocation, result publication, transition to `Review pending`, retakes/reassessment, interviews and credentials.

## Exact-head execution evidence

All acceptance evidence below is bound to `9727d56261d51f7a2c6f61053ccb221d20e529d5`.

- M2.07 targeted TDD gate — **PASS**, run `33440893266`.
- Dedicated M2.07 real Chromium QA — **PASS**, run `33440893171`.
- Hard Browser QA — **PASS**, run `33440893224`.
- Phase 1 retrospective audit — **PASS**, run `33440893250`.
- Full Engineering verification — **PASS**, run `33440893197`, job `99648758412`.
- Full Engineering artifact `9776501359`, digest `sha256:c9749ef22133208eb35e3b4fb3bf0d041e67a4ab49c0a0bef184803b34eae5d8`.
- M2.06 targeted regression — **PASS**, run `33440893256`.
- M2.06 dedicated Chromium regression — **PASS**, run `33440893311`.
- M2.05 targeted regression — **PASS**, run `33440893194`.
- M2.04 targeted regression — **PASS**, run `33440893153`.
- M2.01 targeted regression — **PASS**, run `33440893243`.
- M1.12 targeted regression — **PASS**, run `33440893188`.
- M1.11 targeted regression — **PASS**, run `33440893269`.

## Required RED→GREEN evidence preserved

- Persistence/domain began RED before migration/domain implementation and reached GREEN after direct lineage constraints were corrected.
- Atomic begin began RED before repository/service implementation; forced post-generation failure proves M2.05 form creation joins the outer M2.07 transaction.
- Answer progression/concurrency began RED before submission logic and reached GREEN for all six types, commit-before-next, replay, conflict, final-submit and persistence-failure rollback.
- Worker UI/action contracts began RED before the real start/attempt workspace implementation.
- Browser contract began RED before the M2.07 Playwright harness/workflow; two harness-only defects were isolated without weakening product assertions.
- Rollback vocabulary was forced RED before the history-safe `NOT VALID` restoration was implemented.
- Permanent Engineering wiring was forced RED before the M2.07 checker/package/aggregate gate was added.
- Server→Client attempt metadata leakage was caught by Gatekeeper review, reproduced by a new RED client-boundary contract, then fixed with a field-by-field allowlisted projection and stricter Chromium response scanning.
- Committed-answer mutability was caught by Gatekeeper review, reproduced by a direct database UPDATE/DELETE RED test, then fixed with an append-only database trigger and symmetric rollback cleanup.

## Independent inspection record

### Architecture / code — PASS

- Attempt lifecycle is isolated in `src/lib/assessment-attempt/**` and uses M2.05 immutable forms plus M2.06 eligibility rather than duplicating those authorities.
- Begin and submit mutations execute inside explicit database transactions with row locking/idempotency guards.
- Final submission does not calculate correctness or trigger review/scoring state.
- Permanent Engineering runs M2.07 source contracts and transactional runtime suites independently.

### Security / data integrity — PASS

- Worker identity always comes from the live principal; browser requests cannot supply Worker account, form or blueprint authority.
- Current-question queries select pinned prompt/options/safe metadata only and never select answer keys or rubrics.
- The Server Component projects a field-by-field `AssessmentAttemptClientView`; the internal attempt aggregate never crosses into the client workspace.
- Browser QA scans rendered HTML and captured response bodies for answer keys, rubrics, scoring/correctness and internal attempt metadata (`workerAccountId`, `formId`, `startedAt`).
- Committed answer rows are protected from direct UPDATE/DELETE at the database boundary.
- Cross-form/question-version substitution is constrained by composite foreign keys.
- Audit metadata contains identifiers/count/status only and no committed answer content.

### UI / workflow — PASS

- `GET /worker/available-assessments` remains read-only and only eligible cards expose the POST `Start assessment` action.
- The assessment page requires a live Worker permission and reads only the signed-in Worker's owned attempt.
- The workspace renders exactly one current question, all six canonical input controls, pending disable, `Next`/`Submit assessment`, and no Previous/edit control.
- Dedicated Chromium proves normal Worker login, start, MCQ commit-before-next, written-question reload recovery, cross-Worker denial and final submitted receipt.

### Scope / stale / temporary / regression — PASS

- Final PR diff review found no M2.08/M2.09/M2.10 implementation, no `Review pending` transition, no browser Worker/form/scoring authority and no GET mutation.
- No unresolved PR #91 review thread or submitted review remains at acceptance.
- No temporary diagnostic or test weakening was accepted as a production fix.
- M2.05/M2.06 and inherited browser/retrospective gates remain green on the exact evidence head.

## Known non-blocking limitations

- PR #91 is not merged at certificate issuance.
- M2.07 reload recovery covers committed state only; uncommitted answer autosave/recovery belongs to M2.08.
- Integrity/proctoring and secure-window behavior belong to M2.09.
- Correctness, scoring, pass/fail, written review and transition beyond `Assessment in progress` belong to M2.10.
- Live email/SMS/private-object-storage/malware/liveness/video/payment providers remain external production-activation work outside this milestone.

## Gatekeeper decision

`ACCEPT`

M2.07 satisfies its frozen work contract at the committed candidate-assessment boundary. The exact code evidence head passed dedicated runtime/browser, inherited browser/retrospective and Full Engineering verification, and independent architecture/security/UI/data-integrity/scope review found no remaining blocker.

The certificate itself is governance metadata added after the accepted code evidence head; the resulting metadata head must still receive fresh exact-head verification before PR #91 is merged.

**State distinction:**

- `IMPLEMENTED`: yes
- `TESTED`: yes
- `VERIFIED`: yes
- `ACCEPTED`: yes
- `MERGED`: no at certificate issuance
- `POST-MERGE VERIFIED`: no at certificate issuance
- `RELEASE_READY`: no full-product claim
